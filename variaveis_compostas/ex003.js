var num = [2,4,6,8]
console.log (`Valor ${num}`)

    console.log (`Na primeira posição está o valor ${num[0]}`)
    console.log (`Na segunda posição está o valor ${num[1]}`)

    for (let pos in num) 
    console.log (`O valor ${num[pos]} está na posição ${pos}`)